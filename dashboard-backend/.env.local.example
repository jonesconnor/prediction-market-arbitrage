# FastAPI backend local configuration
REDIS_URL="redis://<url>:<port>"

# Optional overrides; defaults from config.py shown for reference.
# MIN_EDGE -> minimum (1 - sum(outcome_prices) required to surface.
# MIN_LIQUIDITY -> minimum USD liquidity threshold required to surface.
PM_GAMMA_URL="https://gamma-api.polymarket.com/markets"
# PM_CLOB_HOST="https://clob.polymarket.com"
MIN_EDGE="0.00"
MIN_LIQUIDITY="0"
REST_REFRESH_SEC="30"
PM_GAMMA_LIMIT="500"
REST_TIMEOUT_SEC="10"
# EMBEDDING_MODEL="text-embedding-3-small"
# EMBEDDING_BATCH_SIZE="32"
# EMBEDDING_REFRESH_SEC="900"
# EMBEDDING_BATCH_SLEEP_SEC="0"
# SIMILARITY_THRESHOLD="0.75"
# MAX_MATCHES_PER_MARKET="10"
OPENAI_API_KEY=""
# WS_VERIFY_SSL="0"  # Set to '0' to disable websocket SSL verification for local testing only.
# Allow frontend origin (comma-separated). Use '*' for all.
CORS_ALLOW_ORIGINS="<url>"
